export declare class Generator {
    static generateRandomNumber(min: number, max: number): number;
    static generateRandomNColor(min: number, max: number): Color;
    static generateRandomString(lang: string, length: number): string;
    static generateSlug(text: string, separator: string): string;
}
